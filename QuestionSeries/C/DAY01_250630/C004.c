// 연산자
#include <stdio.h>

main()
{
    int x;
    int y;

    x = 10;
    y = x -5;
    printf("x: %d, y: %d \t",x,y);

    if(x>y)
    {
        printf("X value is more bigger than Y.");
    }
    else
    {
        printf("Y value is more bigger than Z.");
    }
}
